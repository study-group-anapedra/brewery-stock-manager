# --- SEGURAN�A E SCHEMA ---
# Garante que o Hibernate n�o apague ou altere seus dados de produ��o
spring.jpa.hibernate.ddl-auto=none 
# Ativa o Flyway para gerenciar as migra��es (conforme seu pom.xml)
spring.flyway.enabled=true

# --- CONEXÃO COM O BANCO DE DADOS (RDS) ---
# Adicionamos a porta 5432 e o nome do banco de produção (stockmanagerprod)
spring.datasource.url=jdbc:postgresql://${DB_URL}:5432/stockmanagerprod
# Nomes das variáveis que devem ser injetadas pelo script de inicialização
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- PERFORMANCE E LOGS ---
# Mantemos false para n�o sobrecarregar os logs em produ��o
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# --- MONITORAMENTO (ACTUATOR) ---
# Alinhado com a configura��o que vimos no seu application.yml
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always

